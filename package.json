{
  "name": "supercash-mobile-app",
  "version": "1.0.0",
  "description": "Capacitor wrapper for SuperCash",
  "main": "index.js",
  "scripts": {
    "dev": "cap copy && cap open android",
    "build": "cap copy && cap sync",
    "sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
